import torch


def build_conv(
    in_channels: int,
    out_channels: int,
    kernel_size: int | tuple,
    stride: int | tuple,
    padding: int | tuple,
) -> torch.nn.Sequential:
    return torch.nn.Sequential(

    )


def build_tconv(
    in_channels: int,
    out_channels: int,
    kernel_size: int | tuple,
    stride: int | tuple,
    padding: int | tuple,
) -> torch.nn.Sequential:
    return torch.nn.Sequential(

    )
